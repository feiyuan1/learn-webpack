import react from 'react'
import SyncTest from './SyncTest'
// import asyncTestIndex from './AsyncTest'

console.log('test: ', typeof react)
document.body.appendChild(SyncTest())
// asyncTestIndex().then(element => document.body.appendChild(element))
